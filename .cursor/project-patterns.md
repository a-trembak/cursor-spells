# Project patterns

> Generated by engineer-review for this repository. Update when conventions drift.
> Path in consumer projects: `.cursor/project-patterns.md`

## Stack

- Primary: kit markdown + bash + small python helpers (not react-web / react-native / java-spring)
- Package manager / build: bash scripts; contract tests under `scripts/tests/`

## Naming

- Files: kebab-case for skills, commands, scripts (`pipeline-status.sh`, `hitl-choice`)
- Components / classes: N/A
- Hooks / utilities / packages: `pg_*` for `pipeline-gates.sh`; `ps__*` private helpers in `pipeline-status.sh`
- Tests: `scripts/tests/<topic>-test.sh` with `assert_eq` / `assert_contains` and `ALL PASS`

## Folder structure

- Source roots: `agents/`, `skills/`, `commands/`, `scripts/`, `docs/superpowers/`
- Feature vs layer layout: pipeline stages documented in `docs/superpowers/pipeline-flow.md` + HTML twin
- Colocation rules (styles, tests, stories): tests live under `scripts/tests/`; dogfood under `docs/superpowers/dogfood/`
- **No pipeline dogfood on this kit:** never `/csp-start-*` / Pipeline route to develop cursor-spells itself (rule `kit-no-pipeline-dogfood`). Manual dogfood checklists remain OK.

## Components / classes

- Preferred component style (function, hooks, etc.): N/A
- State / data-fetching patterns: disk markers under `.cursor/gates/<kind>/<slug>`; trajectory ledgers under `.cursor/gates/trajectory-run/`
- Backend layering (controllers, services, repos) if applicable: N/A

## Packages and imports

- Path aliases: N/A
- Allowed shared packages: source `pipeline-gates.sh` from sibling scripts; do not duplicate slug/path helpers
- Import order / barrel files: N/A

## Code techniques

- Error handling: `set -euo pipefail`; CLI unknown args exit 2; missing resolver → one-sentence skip in skills
- Async patterns: N/A
- Validation: contract tests assert JSON keys and HTML/CSS class tokens
- Logging: human English skeleton on stdout; agents adapt via `plain-language-chat`

## Do not reinvent

List existing helpers/modules that new code must reuse:

- `scripts/pipeline-gates.sh` — gate list/slug/path normalize (`pg_list_gates`, `pg_normalize_plan_path`)
- `skills/hitl-choice/` — closed-set human asks; orientation strip before the question
- `skills/pipeline-status/` — strip template and resolver invocation contract
- `docs/superpowers/pipeline-flow.html` / `pipeline-flow.md` — canvas twin; keep query-param contract in sync with tests

## Comments policy

This project follows `skills/code-comments/SKILL.md`'s Keep / Remove taxonomy as-is. Note any project-specific exceptions here:

- Exceptions (if any): none

## Graphify (preferred when present)

- Enabled: no
- Report path: `graphify-out/GRAPH_REPORT.md`
- Review uses [graphify-protocol.md](../skills/engineer-review/references/graphify-protocol.md): prefer query / report for scoping; never required

## Auth / membership scope (RTK)

N/A for this kit repository.

## Interaction replay (host widgets)

N/A for this kit repository.

## Compact tables / narrow viewports

N/A for this kit repository (static HTML canvas is documentation, not product tables).

## Review learnings (project-private)

- Ledger path: `.cursor/review-learnings.md` (create only after `project_secret`; template in kit)
- Protocol: kit `skills/engineer-review/references/review-learn-protocol.md`
- Kit seed always loaded: `skills/engineer-review/references/learned-misses.md`
- Shareable misses: skill `teach-review`. Local ledger only when the human chooses `project_secret` (client names that must not enter the kit)
